import StockTransferPage from '@/components/stock-transfers/StockTransferPage';

export const metadata = { title: 'Stock Transfer — ERP Finance' };

export default function StockTransfersRoute() {
  return <StockTransferPage />;
}
