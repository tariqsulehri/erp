import WarehouseManagementPage from '@/components/warehouses/WarehouseManagementPage';

export const metadata = { title: 'Warehouses — ERP Finance' };

export default function WarehousesRoute() {
  return <WarehouseManagementPage />;
}
